            auto tasks = getTaskList();
            if (tasks.empty()) {
                std::cout << "List is empty" << std::endl;
            } else {
                int count = 1;
                for (const auto& task : tasks) {
                    std::cout << count << ". " << task << "\n";
                    count++;
                }
            }
